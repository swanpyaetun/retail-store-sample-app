# Default values for carts-dynamodb.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: default
resources:
  requests:
    cpu:
    memory:
  limits:
    cpu:
    memory:

# Default values for checkout-redis.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: default
resources:
  requests:
    cpu:
    memory:
  limits:
    cpu:
    memory:
